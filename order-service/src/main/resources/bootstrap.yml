
# 服务的名称
spring:
  application:
    name: order-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      profile: dev
      label: master
#指定注册中心
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
