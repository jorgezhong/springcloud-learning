spring:
  application:
    name: api-gateway
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      profile: dev
      label: master

#指定注册中心
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/


